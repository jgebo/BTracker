@model IEnumerable<BTracker.RoleViewModel>

@{
    ViewBag.Title = "ListRoles";
}

<h2>System Roles</h2>


    @*@Html.ActionLink("Add Role", "CreateRole")*@
 
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleName)
        </td>
        <td>
            @Html.ActionLink("Assign Users | ", "AssignUserRole", new { roleid=item.RoleId }) 
            @Html.ActionLink("Unassign Users", "UnassignUserRole", new { roleid=item.RoleId }) 
       
            @*@if (item.RoleName != "Administrator" && item.RoleName != "Developer" && item.RoleName != "Submitter" && item.RoleName != "Project Manager" && item.RoleName != "Unassigned")
            {
                @Html.ActionLink(" | Edit Role | ", "EditRole", new { roleid = item.RoleId }) 
                @Html.ActionLink("Delete Role", "DeleteRole", new { roleid = item.RoleId });
            }*@
        </td>
    </tr>
}

</table>
